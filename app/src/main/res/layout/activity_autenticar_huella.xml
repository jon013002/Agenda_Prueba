<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".Autenticar_Huella">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:src="?android:attr/fingerprintAuthDrawable"
        android:background="@color/black"
        />

    <TextView
        android:id="@+id/estadoAutenticar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@color/black"
        android:textSize="15dp"
        android:text="¡Click para iniciar la autenticación!" />

    <Button
        android:id="@+id/btnAutenticacion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/estadoAutenticar"
        android:backgroundTint="@color/colorPrimary"
        android:textColor="@color/white"
        android:layout_centerInParent="true"
        android:layout_marginTop="35dp"
        android:drawableStart="?android:attr/fingerprintAuthDrawable"
        android:drawablePadding="15dp"
        android:text="Autenticacion" />

</RelativeLayout>